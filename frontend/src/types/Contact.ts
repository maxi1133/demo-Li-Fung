interface Contact {
  contact_id?: string,
  contact_name?: string,
  description?: string,
  phone?: string,
  address?: string,
  company?: string,
  city?: string,
  state?: string,
  country?: string
}
